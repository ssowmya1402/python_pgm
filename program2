def rem_vowel(string):
  res=""
  for i in range(len(string)):
    if string[i] not in vowels:
      res = res+string[i]
  return res

string = input("Enter string: ")
vowels = ['a', 'e', 'i', 'o', 'u', 'A', 'E','I','O','U']
print("After removing vowels:",rem_vowel(string))
